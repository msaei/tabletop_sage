# frontend/Dockerfile  (STARTER)
# =================================
# TODO: Complete this Dockerfile so the frontend can run inside Docker.
# Refer to Module 8's Dockerfile Creation lesson for guidance.
#
# Steps to implement:
#   1. Choose a base image — python:3.11-slim is a good choice
#   2. Set a working directory (e.g. /app)
#   3. Copy requirements.txt and install dependencies with pip
#   4. Copy the rest of the source code
#   5. Expose the port streamlit will listen on (8501)
#   6. Set the CMD to start streamlit
#      Hint: streamlit run app.py --server.port=8501 --server.address=0.0.0.0

# TODO: FROM ...

# TODO: WORKDIR ...

# TODO: COPY requirements.txt .
# TODO: RUN pip install ...

# TODO: COPY . .

# TODO: EXPOSE ...

# TODO: CMD [...]
