"""
Capstone Frontend — Streamlit UI  (STARTER)
==============================================
Run locally:
    BACKEND_URL=http://localhost:8000 streamlit run app.py

Inside Docker Compose: reads BACKEND_URL from the environment automatically.

This file is intentionally empty. Design your UI around whatever endpoints
you built in the backend (chat interface, search, CRUD forms, etc.).

Required, at minimum:
    - Connects to the backend API (use api_client.py, don't call requests
      directly from here)
    - A chat or search interface for your RAG pipeline
    - Handles errors and loading states gracefully (no unhandled exceptions
      shown to the user)

TODO: build your page layout, session state, and API calls.
"""

# TODO: import streamlit as st
# TODO: from api_client import ...

# TODO: st.set_page_config(...)
# TODO: initialize st.session_state keys before reading them
# TODO: build your sidebar / main content / chat input
