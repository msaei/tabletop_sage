"""
Capstone Frontend — API Client  (STARTER)
============================================
Wrap all calls to the backend here (auth, CRUD, /ask, /health) so app.py
stays focused on UI. Read BACKEND_URL from the environment.

TODO: implement thin wrapper functions around `requests` for each endpoint
      your backend exposes.
"""

# TODO: import os, requests
# TODO: BACKEND_URL = os.environ.get("BACKEND_URL", "http://localhost:8000")

# TODO: def login(username: str, password: str) -> str: ...       # returns token
# TODO: def ask(question: str, token: str) -> dict: ...
# TODO: def health_check() -> dict: ...
